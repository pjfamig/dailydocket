<h3>Recent Comments</h3>

<% if @feed_items.any? %>
  <ol class="list-unstyled comments">
	<%= nested_comments @feed_items.arrange(:order => :created_at) %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
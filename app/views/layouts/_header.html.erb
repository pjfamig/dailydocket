<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle pull-left" data-toggle="collapse" data-target="#collapsed-content">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">DailyDocket</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="collapsed-content">
		<ul class="nav navbar-nav navbar-right tabs">
			<% if signed_in? && current_user.admin? %>
            <li class="<%= 'active' if current_page?(admin_index_path) %>"><%= link_to "Admin", admin_index_path %></li>
			<% end %>
			
			<% if signed_in? %>
			<li>
				<a href=""><span class="glyphicon glyphicon-inbox"></span></a>
			</li>
			<li>
				<%= link_to current_user.reputation_for(:karma).to_i.to_s + " points", current_user %>			
			</li>	
			<li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
				<%= gravatar_for current_user,  size: 24 %>
                <%= current_user.username %>
 				<b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
			<% else %>
		    <li class="<%= 'active' if current_page?(signin_path) %>"><%= link_to "Sign in", signin_path %></li>
			<li class="<%= 'active' if current_page?(signup_path) %>"><%= link_to 'Sign up', signup_path %></li>

          <% end %>
			<!--<li>
				<button class="btn btn-primary btn-sm submit" data-toggle="modal" data-target="#myModal">Submit</button>
			</li>-->
		</ul>
	</div>
   </div> <!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
